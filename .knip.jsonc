// DOCS https://knip.dev/overview/configuration
{
	"entry": ["src/main.ts"],
	"project": ["**/*.ts"],
	"ignoreDependencies": ["markdownlint-cli"], // used only in pre-commit hook / task,

	"$schema": "https://unpkg.com/knip@5/schema-jsonc.json"
}
